{# app/Resources/views/content/404-page.html.twig #}


{% extends 'base.html.twig' %}

{% block body %}

    {% if message %}
        <div class="alert alert-primary message_nk" role="alert">{{ message }}</div>
    {% endif %}

    <div class="album py-5 bg-light">
        <div class="container">
            <h2>... А сдесь можно разместить вашу рекламму :) ...</h2>

            {% if data is defined %}
                <div>{{ data }}</div>
            {% endif %}

        </div>
    </div>

{% endblock %}
